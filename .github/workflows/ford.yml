name: ForDoc

on:
  push:
    branches:
      - 'main'

jobs:
  documentation:
    permissions:
      contents: write
    uses: apes-suite/reus/.github/workflows/ford.yml@v2.0.0
    with:
      name: Ateles
      docdir: 'build/ford/atl'
