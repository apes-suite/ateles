name: Compilation and Utests

on:
  pull_request:
    branches: [ "main" ]
  merge_group:
    types: [checks_requested]

jobs:
  compilation:
    permissions:
      checks: write
      pull-requests: write
    uses: apes-suite/reus/.github/workflows/compile.yml@v1
    with:
      name: Ateles
